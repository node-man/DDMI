{
  "name": "aimux",
  "version": "0.1.0",
  "description": "AI CLI multiplexer — route prompts to Claude, Codex, Gemini, Ollama with fallback chain",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "keywords": ["ai", "cli", "claude", "codex", "gemini", "ollama", "mcp", "llm"],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "peerDependencies": {
    "@xenova/transformers": "^2.17.0"
  },
  "peerDependenciesMeta": {
    "@xenova/transformers": {
      "optional": true
    }
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
